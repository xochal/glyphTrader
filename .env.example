TFT_HTTPS_PORT=443
TFT_HTTP_PORT=80
# ADMIN_PASSWORD=  # Optional: set password before first run (consumed once)

# GITHUB_TOKEN=  # Optional: GitHub PAT (read-only) for update checks on private repos

# Tradier credentials are entered via the Settings page after first login.
# They are stored encrypted in SQLite, not in this file.
